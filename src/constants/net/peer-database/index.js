import * as POTENTIAL_PEER_LAST_CONNECTION_DISPOSITION from './potential-peer-last-connection-disposition';

/** @typedef {keyof typeof POTENTIAL_PEER_LAST_CONNECTION_DISPOSITION} _PotentialPeerLastConnectionDispositionKeys */

/** @typedef {
	typeof POTENTIAL_PEER_LAST_CONNECTION_DISPOSITION[_PotentialPeerLastConnectionDispositionKeys]
} e_POTENTIAL_PEER_LAST_CONNECTION_DISPOSITION */

// eslint-disable-next-line import/prefer-default-export
export { POTENTIAL_PEER_LAST_CONNECTION_DISPOSITION };
